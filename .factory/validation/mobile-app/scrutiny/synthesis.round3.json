{
  "milestone": "mobile-app",
  "round": 3,
  "status": "fail",
  "validatorsRun": {
    "test": {
      "passed": true,
      "command": "set -e; ROOT=\"/Users/lubomirmolin/PhpstormProjects/codex-mobile-companion\"; if [ ! -f \"$ROOT/apps/mobile/pubspec.yaml\" ] && [ ! -f \"$ROOT/Cargo.toml\" ] && [ ! -d \"$ROOT/apps/mac-shell\" ]; then echo \"workspace not scaffolded yet\"; exit 0; fi; if [ -f \"$ROOT/apps/mobile/pubspec.yaml\" ]; then (cd \"$ROOT/apps/mobile\" && flutter test --concurrency=5); fi; if [ -f \"$ROOT/Cargo.toml\" ]; then cargo test --manifest-path \"$ROOT/Cargo.toml\" --workspace --jobs 5; fi; if [ -f \"$ROOT/apps/mac-shell/CodexMobileCompanion.xcodeproj/project.pbxproj\" ]; then xcodebuild test -project \"$ROOT/apps/mac-shell/CodexMobileCompanion.xcodeproj\" -scheme CodexMobileCompanion -destination 'platform=macOS'; fi",
      "exitCode": 0
    },
    "typecheck": {
      "passed": true,
      "command": "ROOT=\"/Users/lubomirmolin/PhpstormProjects/codex-mobile-companion\"; if [ ! -f \"$ROOT/apps/mobile/pubspec.yaml\" ] && [ ! -f \"$ROOT/Cargo.toml\" ] && [ ! -d \"$ROOT/apps/mac-shell\" ]; then echo \"workspace not scaffolded yet\"; exit 0; fi; if [ -f \"$ROOT/apps/mobile/pubspec.yaml\" ]; then flutter analyze \"$ROOT/apps/mobile\"; fi; if [ -f \"$ROOT/Cargo.toml\" ]; then cargo check --manifest-path \"$ROOT/Cargo.toml\" --workspace --all-targets; fi; if [ -f \"$ROOT/apps/mac-shell/CodexMobileCompanion.xcodeproj/project.pbxproj\" ]; then xcodebuild -project \"$ROOT/apps/mac-shell/CodexMobileCompanion.xcodeproj\" -scheme CodexMobileCompanion -destination 'platform=macOS' build >/dev/null; fi",
      "exitCode": 0
    },
    "lint": {
      "passed": true,
      "command": "set -e; ROOT=\"/Users/lubomirmolin/PhpstormProjects/codex-mobile-companion\"; if [ ! -f \"$ROOT/apps/mobile/pubspec.yaml\" ] && [ ! -f \"$ROOT/Cargo.toml\" ] && [ ! -d \"$ROOT/apps/mac-shell\" ]; then echo \"workspace not scaffolded yet\"; exit 0; fi; if [ -f \"$ROOT/apps/mobile/pubspec.yaml\" ]; then flutter analyze \"$ROOT/apps/mobile\"; fi; if [ -f \"$ROOT/Cargo.toml\" ]; then cargo fmt --manifest-path \"$ROOT/Cargo.toml\" --all --check && cargo clippy --manifest-path \"$ROOT/Cargo.toml\" --workspace --all-targets -- -D warnings; fi; if [ -f \"$ROOT/apps/mac-shell/CodexMobileCompanion.xcodeproj/project.pbxproj\" ]; then xcodebuild -project \"$ROOT/apps/mac-shell/CodexMobileCompanion.xcodeproj\" -scheme CodexMobileCompanion -destination 'platform=macOS' build >/dev/null; fi",
      "exitCode": 0
    }
  },
  "reviewsSummary": {
    "total": 2,
    "passed": 0,
    "failed": 2,
    "failedFeatures": [
      "integration-mobile-runtime-sync-and-safe-affordances-fix",
      "integration-mobile-persistence-and-auto-reconnect-fix"
    ]
  },
  "blockingIssues": [
    {
      "featureId": "integration-mobile-runtime-sync-and-safe-affordances-fix",
      "severity": "blocking",
      "description": "Branch-switch approvals still cannot show the exact target branch under the real bridge contract because `approvalTargetLabel()` treats `ApprovalRecordDto.target` as a concrete branch name while the bridge still emits the generic `git.branch_switch` identifier for real approval payloads."
    },
    {
      "featureId": "integration-mobile-runtime-sync-and-safe-affordances-fix",
      "severity": "blocking",
      "description": "Notification preference toggles still only persist local UI state and affect thread badge/banner chrome; they do not control any actual approval or live-activity notification delivery/suppression path, so the runtime-notification requirement remains unmet."
    },
    {
      "featureId": "integration-mobile-persistence-and-auto-reconnect-fix",
      "severity": "blocking",
      "description": "`apps/mobile/lib/features/pairing/application/pairing_controller.dart` still suppresses follow-up reconnect timers because `_restoreTrustedBridgeSession()` calls `_scheduleReconnect()` while `_isReconnectInProgress` remains true, leaving trusted-session recovery limited to a single automatic retry."
    },
    {
      "featureId": "integration-mobile-persistence-and-auto-reconnect-fix",
      "severity": "blocking",
      "description": "`apps/mobile/lib/features/threads/application/thread_detail_controller.dart` still drops follow-up reconnect/catch-up scheduling because failure-path reschedules happen before `_isReconnectInProgress` is cleared, so selected-thread recovery still stops after one automatic retry during longer outages."
    },
    {
      "featureId": "integration-mobile-persistence-and-auto-reconnect-fix",
      "severity": "blocking",
      "description": "`apps/mobile/lib/features/threads/application/thread_list_controller.dart` still discards retry rescheduling while reconnect work is marked in progress, so cached thread-list restoration and selected-thread relaunch recovery can still require a manual refresh after the first timed retry fails."
    }
  ],
  "appliedUpdates": [],
  "suggestedGuidanceUpdates": [
    {
      "target": "skill:integration-worker",
      "suggestion": "Require integration workers to read the relevant `.factory/library/` contract/reference notes for the surfaces they touch, not only `user-testing.md`, before implementing cross-surface fixes.",
      "evidence": "The current re-review of `integration-mobile-runtime-sync-and-safe-affordances-fix` confirmed the worker read `user-testing.md` but not `.factory/library/api.md`, then implemented/tests-assumed an approval DTO shape that still disagrees with the documented bridge contract for branch-switch approval targets.",
      "isSystemic": true
    },
    {
      "target": "skill:integration-worker",
      "suggestion": "Tighten the integration-worker handoff rules so required lifecycle/manual verification must either include recorded interactive observations or be explicitly called out as a procedure deviation.",
      "evidence": "The re-review again found that the integration-worker skill requires manual lifecycle verification, but the referenced worker transcript/handoff recorded only automated commands while still marking `followedProcedure: true`.",
      "isSystemic": true
    }
  ],
  "rejectedObservations": [],
  "previousRound": ".factory/validation/mobile-app/scrutiny/synthesis.round2.json"
}
